name: ci

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    name: vet, build, race, coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: gofmt
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then echo "::error::gofmt needed on: $out"; echo "$out"; exit 1; fi
      - run: go vet ./...
      - run: go build ./...
      - run: go test -race ./...
      - name: coverage gate (>80%, cross-package)
        run: |
          # -count=1 disables the test cache so the cross-package coverage
          # profile is assembled from one fresh run of every test binary; mixing
          # cached and fresh binaries under -coverpkg produces an incomplete profile.
          go test -count=1 -coverpkg=./... -coverprofile=coverage.out ./... >/dev/null
          go run ./tools/covercheck coverage.out

  integration:
    name: integration (docker dataplane)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - run: go test -tags=integration ./test/integration -count=1 -timeout 10m -v
