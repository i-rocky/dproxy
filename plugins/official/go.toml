schema = 1
name = "go"
bins = ["go", "gofmt"]
commands = { go = ["go"], gofmt = ["gofmt"] }
[images.default]
repository = "docker.io/library/golang"
tag_template = "{version}"
[[caches]]
path = "/go/pkg/mod"
compatibility = "minor"
[[platforms]]
os = "linux"
arch = "amd64"
