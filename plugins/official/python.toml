schema = 1
name = "python"
bins = ["python", "python3", "pip", "uv"]
commands = { python = ["python"], python3 = ["python3"], pip = ["pip"], uv = ["uv"] }
[images.default]
repository = "docker.io/library/python"
tag_template = "{version}"
[[caches]]
path = "/root/.cache"
compatibility = "minor"
[[platforms]]
os = "linux"
arch = "amd64"
